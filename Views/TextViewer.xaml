<Window x:Class="LargeFolderFinder.TextViewer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Text Viewer" Height="600" Width="800" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="ContentTextBox" Grid.Row="0" Margin="10" IsReadOnly="True" 
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Courier New, monospace" FontSize="14" TextWrapping="Wrap" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <Button x:Name="CopyButton" Width="30" Margin="0,0,10,0" Click="CopyButton_Click">
                <Path Data="M19,21H8V7H19M19,5H8A2,2 0 0,0 6,7V21A2,2 0 0,0 8,23H19A2,2 0 0,0 21,21V7A2,2 0 0,0 19,5M16,1H4A2,2 0 0,0 2,3V17H4V3H16V1Z" Fill="#555" Stretch="Uniform" Margin="4"/>
            </Button>
            <Button x:Name="OpenFolderButton" Content="Open Folder" Width="100" Margin="0,0,10,0" Click="OpenFolderButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Width="100" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
