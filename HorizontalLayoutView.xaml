<!-- Horizontal Layout View -->
<UserControl x:Class="LargeFolderFinder.HorizontalLayoutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LargeFolderFinder"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>    <!-- Main Content Area -->
            <RowDefinition Height="Auto"/> <!-- Footer Area (Full Width) -->
        </Grid.RowDefinitions>

        <!-- Main Content (Settings Left, Results Right) -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" MaxWidth="300" MinWidth="250"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>

            <!-- Left column: Settings Area -->
            <Grid Grid.Column="0" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Row 0: View Label -->
                    <RowDefinition Height="Auto"/> <!-- Row 1: Display Settings -->
                </Grid.RowDefinitions>

                <!-- Row 0: View Label Header -->
                <TextBlock x:Name="viewHeaderLabel" Grid.Row="0" Text="表示" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,0,5" FontSize="12"/>

                <!-- Row 1: Display Settings (Vertical & Indented) -->
                <StackPanel Grid.Row="1" Orientation="Vertical" Margin="15,0,0,0">
                    <!-- Font Size -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Font:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="fontSizeTextBox" Width="30" Height="24" VerticalContentAlignment="Center" Text="14" />
                    </StackPanel>
                    <!-- View Files -->
                    <StackPanel x:Name="includeFilesPanel" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                        <TextBlock x:Name="includeFilesLabel" Text="ファイルも表示" VerticalAlignment="Center" Margin="0,0,3,0"/>
                        <CheckBox x:Name="includeFilesCheckBox" VerticalAlignment="Center"/>
                    </StackPanel>
                    <!-- Size Threshold Input -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock x:Name="minSizeLabel" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="minSizeTextBox" Width="60" Height="26" VerticalContentAlignment="Center" Text="1" Margin="0,0,5,0"/>
                        <ComboBox x:Name="unitComboBox" Width="60" Height="26" VerticalContentAlignment="Center">
                        </ComboBox>
                    </StackPanel>
                    <!-- Sort -->
                    <StackPanel x:Name="sortOuterPanel" Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock x:Name="sortLabel" Text="ソート:" VerticalAlignment="Center" Margin="0,0,5,0" />
                        <ComboBox x:Name="sortComboBox" Width="100" Height="24" VerticalContentAlignment="Center" SelectedIndex="0" Margin="0,0,5,0">
                            <ComboBoxItem>Size</ComboBoxItem>
                            <ComboBoxItem>Name</ComboBoxItem>
                        </ComboBox>
                        <ComboBox x:Name="sortDirectionComboBox" Width="80" Height="24" VerticalContentAlignment="Center" SelectedIndex="1">
                            <ComboBoxItem>Asc</ComboBoxItem>
                            <ComboBoxItem>Desc</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                    <!-- Separator -->
                    <StackPanel x:Name="separatorOuterPanel" Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock x:Name="separatorLabel" Text="空白:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <ComboBox x:Name="separatorComboBox" Width="60" Height="24" VerticalContentAlignment="Center" SelectedIndex="0">
                            <ComboBoxItem>Tab</ComboBoxItem>
                            <ComboBoxItem>Space</ComboBoxItem>
                        </ComboBox>
                    </StackPanel>
                    <!-- Tab Width (Only visible in Tab mode) -->
                    <StackPanel x:Name="tabWidthArea" Orientation="Horizontal" Margin="0,0,0,5">
                        <TextBlock x:Name="tabWidthLabel" Text="幅:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBox x:Name="tabWidthTextBox" Width="30" Height="24" VerticalContentAlignment="Center" Text="4" />
                    </StackPanel>
                </StackPanel>
            </Grid>

            <!-- Right column: Results Area -->
            <Grid Grid.Column="1" Margin="0,10,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Toolbar -->
                    <RowDefinition Height="*"/>    <!-- Text display -->
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5" HorizontalAlignment="Right">
                    <Button x:Name="copyButton" ToolTip="Copy" Width="32" Height="32" Click="CopyButton_Click">
                        <Path Data="{StaticResource CopyIconGeometry}" Fill="#444" Stretch="Uniform" Margin="6" />
                    </Button>
                </StackPanel>
                <!-- Output ListBox (Virtualizing) -->
            <ListBox x:Name="outputListBox" 
                     Grid.Row="1"
                     VirtualizingStackPanel.IsVirtualizing="True"
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                     ScrollViewer.VerticalScrollBarVisibility="Auto" 
                     FontFamily="MS Gothic"
                     FontSize="14"
                     BorderThickness="1"
                     BorderBrush="#ABADB3"
                     AlternationCount="2"
                     SelectionMode="Extended"
                     PreviewKeyDown="ListBox_PreviewKeyDown">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0,0"/>
                        <Setter Property="MinHeight" Value="8"/>
                        <!-- Event Handlers -->
                        <EventSetter Event="MouseDoubleClick" Handler="ListBoxItem_MouseDoubleClick"/>
                        
                        <Style.Triggers>
                            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                                <Setter Property="Background" Value="#FAFAFA"/>
                            </Trigger>
                             <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsExpanded}" Value="False"/>
                                    <Condition Binding="{Binding IsFile}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" Value="#E6F7FF"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" />
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            </Grid>
        </Grid>

        <!-- Footer (Full Width) -->
        <StackPanel Grid.Row="1" Margin="10,0,10,10">
            <Border Height="1" Background="#EEE" Margin="0,0,0,5"/>
            <ProgressBar x:Name="scanProgressBar" Height="4" Margin="0,0,0,2" Visibility="Collapsed" />
            <Grid>
                <TextBlock x:Name="statusTextBlock" Text="準備完了" Foreground="Gray" FontSize="11" />
                <TextBlock x:Name="notificationTextBlock" HorizontalAlignment="Right" Foreground="#0078D7" FontSize="11" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
