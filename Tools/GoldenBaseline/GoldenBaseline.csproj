<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <LangVersion>12.0</LangVersion>
    <AssemblyName>GoldenBaseline</AssemblyName>
    <RootNamespace>LargeFolderFinder.GoldenBaseline</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- 被テストアプリ本体を読み取り専用の依存として参照する（design.md: Allowed Dependencies） -->
    <ProjectReference Include="..\..\LargeFolderFinder.csproj" />
  </ItemGroup>

</Project>
